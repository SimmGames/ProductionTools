using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DialogueSystem;

namespace DialogueSystem
{
    public class DialogueCoder
    {
        // Code to make code

        //Variables will be instantiated at the top of the class.

        // Each Dialogue Node output will hvae a function and the condition will be in the IF statement.

        // Event nodes are the insides of their own functions

        //condition nodes are their own if statements wrapped in functions
    }
}